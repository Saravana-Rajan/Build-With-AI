# Copy to frontend/.env (gitignored) and fill in.

# Base URL of the FastAPI backend.
VITE_API_URL=http://localhost:8000

# Google Maps / map provider key. Leave blank to keep the map placeholder.
VITE_MAPS_API_KEY=
